services:
  app:
    build: .
    command: sh -c "npm run db:migrate && npm run dev"
    ports:
      - "5173:5173"
      - "3001:3001"
    # If dev server is running on same ports, use:
    # - "5174:5173"
    # - "3002:3001"
    volumes:
      - data:/app/data

volumes:
  data:
